A content-based movie recommendation web app built using Python and Streamlit.
It recommends similar movies using cosine similarity.
Movie posters are fetched using the TMDB API.
Run the app using: streamlit run app.py
